NAME=text-types-enum-android
FILE=-
CMDS=<<EOF
!!../bin/r2flutter -T bins/android/mafia 2>/dev/null | grep '^enum AppLifecycleState {'
EOF
EXPECT=<<EOF
enum AppLifecycleState { detached, hidden, inactive, paused, resumed }
EOF
RUN
